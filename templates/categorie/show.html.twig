{% extends 'base.html.twig' %}

{% block title %}Categorie{% endblock %}

{% block body %}
    <h1 class="text-center text-success">Categorie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categorie.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ categorie.titre }}</td>
            </tr>
            <tr>
                <th>Descriptif</th>
                <td>{{ categorie.descriptif }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-primary m-3" href="{{ path('app_categorie_index') }}">back to list</a>

    <a class="btn btn-success m-3" href="{{ path('app_categorie_edit', {'id': categorie.id}) }}">edit</a>

    {{ include('categorie/_delete_form.html.twig') }}
{% endblock %}
